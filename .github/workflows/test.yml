name: Test 

on:
  repository_dispatch:
    types: 
      - custom-event
    workflow-dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Run a script
      run: echo "The action was triggered!"
